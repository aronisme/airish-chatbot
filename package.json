{
  "name": "airish-chatbot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.0",
    "@upstash/redis": "^1.38.0"
  },
  "devDependencies": {
    "vercel": "^34.2.0"
  }
}
